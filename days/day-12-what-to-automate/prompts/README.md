# Day 12 Prompts

Copy-paste prompts for today's lesson. The authoritative versions are in [`../resources.md`](../resources.md) under "Downloads / templates for this day" — pull any specific prompt into its own file here if you want a cleaner clipboard target.

Example:
```bash
# extract the first prompt from resources.md into its own file
awk '/^```$/,0' ../resources.md | head -20 > request-deck.md
```

Or just copy from `resources.md` directly — whatever's faster.
